const router = require('express').Router(); const { body, param } = require('express-validator'); const validate = require('../middleware/validateMiddleware'); const c = require('../controllers/userController'); const { protect, authorize } = require('../middleware/authMiddleware');
router.patch('/me', protect, [body('name').optional().trim().isLength({ min: 2, max: 100 }), body('phone').optional().trim().isLength({ max: 30 })], validate, c.updateMe); router.get('/', protect, authorize('admin'), c.getUsers); router.patch('/:id/status', protect, authorize('admin'), [param('id').isMongoId(), body('isActive').isBoolean()], validate, c.setUserActive); module.exports = router;
